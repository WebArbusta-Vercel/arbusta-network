{
  "name": "arbusta-network",
  "version": "1.0.0",
  "description": "Programa de Referidos · Arbusta",
  "private": true,
  "scripts": {
    "dev": "npx serve .",
    "build": "echo 'No build step needed for static HTML'",
    "lint": "echo 'No linter configured'"
  },
  "dependencies": {
    "@vercel/analytics": "^1.4.1"
  }
}
